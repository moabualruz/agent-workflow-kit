# Claude Workflow Kit

Claude Code pack for Agent Workflow Kit.

Claude is the reference harness. This pack keeps Claude native behavior intact:

- dynamic workflows through the native `Workflow` tool
- saved workflow commands under `.claude/workflows/*.js`
- progress, stop, and resume through `/workflows`
- deep research through Claude's bundled `/deep-research`
