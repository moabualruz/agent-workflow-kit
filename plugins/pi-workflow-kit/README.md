# Pi Workflow Kit

Pi extension pack for Agent Workflow Kit.

Native surfaces:

- TypeScript extension entry under `extensions/`
- `registerCommand` for user commands
- `registerTool` for model-callable tools
- `appendEntry` for Pi-native session persistence
