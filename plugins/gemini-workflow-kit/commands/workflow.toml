description = "Create and run an Agent Workflow Kit workflow"
prompt = """
Call the shared CLI directly:
agent-workflow-kit workflow "<task>" --json
Return the run id, status, and artifact path only.
"""
