description = "Enable, disable, or report Agent Workflow Kit ultracode mode"
prompt = """
Call the shared CLI directly:
agent-workflow-kit ultracode on --json
agent-workflow-kit ultracode off --json
agent-workflow-kit ultracode status --json
Ultracode is an explicit, persisted project toggle (stored in .agent-workflow-kit/config.json). Only turn it on when the user asks. Return the resulting ultracode state only.
"""
