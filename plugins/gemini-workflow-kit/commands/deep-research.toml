description = "Run Agent Workflow Kit deep research"
prompt = """
Call the shared CLI directly:
agent-workflow-kit deep-research "<question>" --json
Return the run id, status, and artifact path only.
"""
